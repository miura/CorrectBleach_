<?xml version="1.0" encoding="UTF-8"?>
<project name="CorrectBleach_" default="" basedir=".">
    <description>BurgerBurge_ build file</description>
  <property name="src" location="src" />
  <property name="build" location="bin" />
  <property name="dist"  location="dist" />
 
	 <property name="pluginsDir"  location="$basedir/../../IJ/plugins/" />
 
  <property name="user.name" value="Kota Miura" ></property>
<target name="main" depends="compress" description="Main target">
	<echo>
		Building the .jar file.
	</echo>
</target>
 <target name="compress" depends="" description="generate the distribution">
 	<jar jarfile="CorrectBleach_.jar">
 		<fileset dir="." includes="plugins.config" />
		<fileset dir="${build}" includes="**/*.*" ></fileset>
 		<manifest>
 		 	 	      <attribute name="Built-By" value="${user.name}"/>
 		</manifest>
 	</jar>
    <copy file="CorrectBleach_.jar" toDir="${pluginsDir}" />
  </target>
</project>